---
description: Show the status of the postgres-app stack containers.
agent: build
---

# PS

Run `make ps` from the repo root and report the container states
(`postgres-app-db`, `postgres-app-pgadmin`).
